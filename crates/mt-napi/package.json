{
  "name": "@7n/mt-napi-build",
  "version": "0.0.0",
  "private": true,
  "description": "Build-обгортка napi-аддона mt (артефакти йдуть у платформні підпакети packages/mt-napi-*)",
  "license": "ISC",
  "scripts": {
    "build": "napi build --platform --release",
    "build:debug": "napi build --platform"
  },
  "devDependencies": {
    "@napi-rs/cli": "^2"
  },
  "napi": {
    "binaryName": "mt",
    "packageName": "@7n/mt-napi",
    "targets": [
      "aarch64-apple-darwin",
      "x86_64-unknown-linux-musl"
    ]
  }
}
