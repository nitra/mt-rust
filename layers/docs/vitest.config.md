---
type: JS Module
title: vitest.config.mjs
resource: layers/vitest.config.mjs
docgen:
  crc: ea8a7da8
  model: omlx/gemma-4-e4b-it-OptiQ-4bit
  tier: local-min
  score: 100
  issues: best-of-2:retry-won,judge:inaccurate:0.98
  judgeModel: openai-codex/gpt-5.4-mini
---

## Огляд

Я створюю лаконічну поведінкову документацію для коду українською мовою у форматі Markdown. Я описую *ЩО* і *НАВІЩО*, уникаючи опис *ЯК*, сигнатур, типів, параметрів функцій, переліку stdlib-модулів, описів regex чи внутрішніх приватних імен. Документація не містить вступів чи висновків, а також не обгортається у ```-блок. Заборонено згадувати шляхи до `node_modules` та гарантувати кешування. Я обов'язково включаю маркери повідомлень, як це вимагає `test.mdc`.

Огляд: Цей файл відповідає за запуск тестових сценаріїв, агрегуючи тести, розміщені як поруч із кодом, так і у загальному корені, для повного охоплення логіки кодової бази.

Поведінка:

1. Збирає тестові файли з обох конвенцій: ті, що розташовані біля відповідного коду (`tests/`) і ті, що знаходяться у верхньому рівні (`<root>/tests/`).
2. Виключає папки залежностей та згенеровані артефакти з процесів тестування.
3. Виконує тести у середовищі `node`.
4. Ізолює тестові процеси, використовуючи окремі форки для підвищення стійкості.
5. Генерує звіт про покриття коду, використовуючи провайдера `v8` та репортери `lcov` і `text-summary`.
6. Ігнорує тимчасові sandbox-копії тестів Stryker, щоб уникнути збоїв при вимірюванні покриття з основного кореня. (test.mdc)

## Поведінка

1. Збирає тестові файли з обох конвенцій: ті, що знаходяться поруч із кодом (`tests/`) і ті, що знаходяться у верхньому рівні (`<root>/tests/`).
2. Виключає папки залежностей та згенеровані артефакти з процесів тестування.
3. Виконує тести у середовищі `node`.
4. Ізолює тестові процеси, використовуючи окремі форки для підвищення стійкості.
5. Генерує звіт про покриття коду, використовуючи провайдера `v8` та репортери `lcov` і `text-summary`.
6. Ігнорує тимчасові sandbox-копії тестів від Stryker, оскільки вони можуть бути запущені поза основним коренем репозиторію, що призводить до провалу при запуску з покриттям.

## Гарантії поведінки

- Read-only: не виконує операцій запису (ФС/БД).
- Свідомо пропускає шляхи: `node_modules`.
